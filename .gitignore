# the ROM (user-provided, never committed) and runtime saves
p.gba
p.sav

# build outputs
poke
poke.html
poke.js
poke.wasm
poke.data

# local-only fluidsynth comparison renders (never commit)
docs/compare/

# regenerable caches (see README: sources of truth)
vendor/
docs/data/
__pycache__/

# leaked original sound source + SC-88Pro soundfont: local tooling inputs only
pm_eme_ose.7z
midi-sc88/
GBApokemonTestLite.sf2

# OS junk
.DS_Store
